{
  "id": "e52f17e3273f",
  "title": "USB-CDC 通信实战应用：树莓派 Pico 虚拟串口 + select+MicroPython 全拆解 - FreakStudio",
  "url": "https://www.cnblogs.com/FreakEmbedded/p/22803743",
  "contentZh": "",
  "contentFull": "\n使用 USB-CDC 类 VCSP 虚拟串口完成数据读写\n虚拟串口是指将 USB 设备模拟成传统的串口设备(COM 口),从而可以使用标准的串口通信协议和软件与之交互。主要特点包括:\n\n提供标准的串口接口和功能,比如波特率、数据位、停止位等设置\n在操作系统中表现为一个标准的串口设备,可以被常规的串口应用程序访问\n使用 CDC(通信设备类)USB 类协议,将 USB 设备的数据通信转换为标准的串口通信\n通常用于需要与旧版软件或设备兼容的场景,比如工控系统、测试设备等\n\n这种虚拟串口的设计,大大提高了 USB 设备的兼容性和易用性,使得老旧系统也可以无缝接入新的 USB 设备。操作系���的 USB 驱动会自动将 USB 设备转换为标准的串口设备,上层应用程序无需修改即可使用。\n以下代码可以在我们提供的资料包中 elegance-devkit v1\\Demo\\39 USB_CDC 文件夹找到。\n示例代码如下：\n# Python env   : MicroPython v1.23.0\n# -*- coding: utf-8 -*-        \n# @Time    : 2024/7/29 下午1:22   \n# @Author  : 李清水            \n# @File    : main.py       \n# @Description : USB CDC类实验，使用Pico上MicroPython固件内置的USB—CDC类完成读写操作\n\n# ======================================== 导入相关模块 ========================================\n\nimport select\nimport sys\nimport time\n\n# ======================================== 全局变量 ============================================\n\n# 记录花费时间的列表\ndelta_list = [0] * 10\n# 记录读取次数\nread_count = 0\n\n# ======================================== 功能函数 ============================================\n\n# ======================================== 自定义类 ============================================\n\n# ======================================== 初始化配置 ==========================================\n\n# 延时等待设备初始化\ntime.sleep(3)\n# 打印调试信息\nprint('FreakStudio : Using USB CDC to read and write data')\n\n# 创建一个 select.poll() 对象,用于监听标准输入数据的到来\npoll_obj = select.poll()\n# 将标准输入 sys.stdin 注册到 poll_obj 对象中,并指定监听类型为 select.POLLIN(表示监听输入事件)\npoll_obj.register(sys.stdin, select.POLLIN)\n\n# ========================================  主程序  ===========================================\n\n# 进入一个无限循环，以下使用select模块完成非阻塞式读写操作\nwhile True:\n\n    # 当读取到 10 次数据后\n    if read_count == 10:\n        # 将read_count 置为 0\n        read_count = 0\n        # 输出10次发送数据耗费的平均时间\n        avg_delta = sum(delta_list) / len(delta_list)\n        print(\"avg time cost : \" + str(avg_delta) + \" ms\\r\")\n\n    # 使用 poll_obj.poll(1) 方法等待标准输入的数据到来，等待超时为 1 ms\n    poll_results = poll_obj.poll(1)\n    if poll_results:\n\n        # 如果有数据到来(即 poll_results 不为空),则从标准输入中读取数据,并将其写入到标准输出\n        # readline()读取标准输入中的一行数据(以换行符结尾)，返回一个包含该行数据的字符串\n        # strip()方法会去除字符串两端的空白字符,包括空格、制表符、换行符等\n        # data = sys.stdin.readline().strip()\n        # sys.stdin.readline().strip()读取为非阻塞式读取\n        # input()为阻塞式读取，会阻塞程序运行\n        data = input(\"please input data : \")\n\n        # 测试数据写出的时间\n        t = time.ticks_us()\n\n        # 将数据写入到标准输出\n        # 也可以使用sys.stdout.write()方法\n        # sys.stdout.write(\"received data : \" + data + \"\\r\")\n        # sys.stdout.write()会直接将数据写入标准输出流,不会进行缓冲\n        # print()会将输出内容先存储在缓冲区中,直到缓冲区满或者遇到换行符时才会真正输出\n        print(\"received data : \" + data + \"\\r\")\n\n        # 计算一次读写花费时间\n        delta = time.ticks_diff(time.ticks_us(), t)/1000\n        # 输出本次读写花费的时间\n        print(\"time cost : \" + str(delta) + \" ms\\r\")\n\n        # 将本次读写花费的时间添加到 delta_list 列表中\n        delta_list[read_count] = delta\n        # 读取次数加1\n        read_count += 1\n\n    else:\n        # 如果没有数据到来(即 poll_results 为空),则继续循环\n        continue\n\n这里，我们通过 select 模块监听标准输入,当有数据到来时使用 print 函数和 input 函数完成读取并写回,同时测量读写耗时并计算平均值，从而模拟 USB 设备与主机之间的数据交互场景。\n烧录代码，打开远程终端，输入数据，可以看到输入数据写回到了终端：\n\n\n这里，为了便于演示，我们使用了阻塞式的 input 方法进行读取，实际数据传输中，我们希望以非阻塞的方式读取标准输入, 以便能够持续监测是否有数据输入，通常使用 sys.stdin.readline().strip()方法读取，二者区别如下：\n\nsys.stdin.readline().strip() 是非阻塞式读取, 当没有数据输入时不会阻塞程序\ninput() 是阻塞式读取, 当没有数据输入时会阻塞程序的执行\n\n在输出接收到的数据中，我们使用了大家熟悉的 print 函数，但是在实际应用中，为保证实时性我们会使用 sys.stdout.write()方法，二者区别如下：\n\nprint() 函数是 Python 内置的标准输出函数，它将输出的内容输出到屏幕上，并自动添加换行符。print()函数会将输出内容先存储在缓冲区中，直到缓冲区满或者遇到换行符时才会真正输出\nsys.stdout.write() 方法是 Python 标准库中的方法，它可以将字符串写入到标准输出流中，不会自动添加换行符，如果你想要立即输出内容而不等待缓冲区满，可以使用 sys.stdout.write()方法\n\n\n\n"
}