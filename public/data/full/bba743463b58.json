{
  "id": "bba743463b58",
  "title": "Rocky‑Linux‑10.2‑x86_64 Stratis 完整部署教程 - 九幽提刑官",
  "url": "https://www.cnblogs.com/mrpan0920/p/22650754",
  "contentZh": "",
  "contentFull": "\nRocky‑Linux‑10.2‑x86_64 Stratis 完整部署教程\nStratis 是 RHEL 系新一代存储管理器，基于 XFS，支持精简配置、自动扩容、快照、多磁盘组成存储池，替代部分 LVM 场景。 ⚠️重要限\n不支持 root 根文件系统，仅用于数据盘；池创建后不能加密，加密只能创建池时指定；磁盘不能已有分区表 / 文件系统签名Red Hat。\n环境准备\n系统：Rocky‑Linux‑10.2‑x86_64 新增磁盘示例：/dev/sdb（10G，用于 stratis 池）；多盘可以 /dev/sdb /dev/sdc\n1. 安装 Stratis 软件包\nStratis 由后台守护进程和命令行工具组成。首先，需要安装 stratisd（提供守护进程）和 stratis-cli（提供命令行工具）\n\n1sudo yum install -y stratisd stratis-cli\n\n启动并设置开机自启\n安装完成后，需要启动 stratisd 服务，并设置其在系统启动时自动运行：\n\n1sudo systemctl enable --now stratisd\n\n 创建存储池 (Pool)\nStratis 的核心概念之一是��储池。你需要将一块或多块未使用的块设备（如 /dev/sdb）添加到池中\n\nsudo stratis pool create mypool /dev/nvme0n2\n \n没有任何报错输出，说明你的 Stratis 存储池 mypool 已经创建成功了！🎉\n接下来，你可以按照以下步骤，继续完成文件系统的创建、挂载以及开机自动挂载的配置：\n1. 验证存储池是否创建成功\n你可以运行以下命令查看当前系统中的 Stratis 池：\n\nsudo stratis pool list\n\n下一步：创建文件系统并挂载\n现在存储池已经就绪，你可以继续完成文件系统的创建和挂载，将其用作 /data 目录：\n 在池中创建文件系统\n\n1sudo stratis fs create mypool myfs\n\n\n挂载到 /data 目录\n\n1sudo mkdir -p /data\n2sudo mount /dev/stratis/mypool/myfs /data\n\n别担心，这个提示并不是报错，而是 systemd 发出的一个非常友好的信息提示（Hint）。\n为什么会出现这个提示？\n当你修改了 /etc/fstab 文件后，systemd 仍然在内存中缓存着旧版本的挂载配置。当你使用 mount 命令时，它检测到配置文件已更新，就会提示你重新加载配置，以确保系统的挂载单元（.mount）与 fstab 保持一致2。\n 如何解决？\n你只需要按照提示，重新加载一下 systemd 的配置即可：\n\nsudo systemctl daemon-reload\n\n执行完这条命令后，提示就会消失。你可以再次运行挂载命令来确认状态：\n\nsudo mount /dev/stratis/mypool/myfs /data\n\n运维避坑指南（关键！）\n在 Linux 运维中，修改 /etc/fstab 后有一个黄金法则：绝对不要直接重启系统！ 在重启之前，务必先运行以下命令进行测试：\n\nsudo mount -a\n\nmount -a 相当于 fstab 的“试运行”命令。它会按文件逐行尝试挂载，如果配置有误（比如 UUID 写错、挂载点不存在等），它会立刻报错，方便你当场修正。如果 mount -a 没有任何报错输出，就说明你的 fstab 配置完全正确，可以放心重启了\n现在，你的 Stratis 存储已经彻底部署完毕并投入使用了。你可以运行以下命令来做最后的验证：\n\n1df -hT /data\n\n完美！🎉 从 df -hT 的输出结果来看，你的 Stratis 存储已经彻底大功告成，并且完美运行！\n\n\n\n\n\n\n\n\n\n\n"
}